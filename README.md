# The-Syntax-Surgeon

CMD

cd /set path/

py -3 -m venv .venv

.venv\scripts\activate

pip install language-tool-python autopep8 pyinstaller

pyinstaller --onefile syntax_surgeon.py

Run Command:

python syntax_surgeon.py

         OR

Click on the Dist / syntax_surgeon.exe
